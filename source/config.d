module config;

/++
        WORLD CONFIG
+/

enum CHUNK_EXTENT_X = 10;
enum CHUNK_EXTENT_Y = 10;

/++
        CHUNK CONFIG
+/

enum CHUNK_SIZE = 16;
enum CHUNK_SIZE_PIXELS = TILE_SIZE*CHUNK_SIZE;

/++
    LIGHTING CONFIG
+/

enum CHUNK_SHADOW_SCALE = 8; //cast(int)(TILE_SIZE/4f);
enum CHUNK_SHADOW_SIZE = CHUNK_SIZE*CHUNK_SHADOW_SCALE;
enum CHUNK_SHADOW_TRC = 256;
enum LIGHT_MIN_BLOCK = 0.05f;

/++
        TILE CONFIG
+/
enum TILE_SIZE = 16;

enum TILE_HB_SHRINK = 4;
enum HIT_SCALE_EFF_MAX = 2f;
enum HIT_SCALE_FALLOFF = 1f;

/++
        WORLDGEN CONFIG
+/
enum H_HEIGHT_FACTOR = 6;
enum H_HEIGHT_LIMIT = (CHUNK_SIZE*H_HEIGHT_FACTOR);
enum H_SMOOTH_FACTOR = 256;
enum HARDSAND_START = 64;

/++
        PLAYER CONFIG
+/

enum PLAYER_SPEED = 1.8f;
enum PLAYER_JUMP_SPEED = 1.5f;
enum GRAVITY_CONST = .4f;
enum MAX_SPEED = 14f;
enum DRAG_CONST = 0.7f;
enum JUMP_TIMER_START = 18;


enum PLAYER_COL_PRECISION = 25;